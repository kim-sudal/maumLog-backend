{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Containers: Docker Compose",
            "type": "shell",
            "command": "docker compose -f docker-compose.yml up -d --build",
            "problemMatcher": []
        }
    ]
}
